
 <header class="intro">
        <div class="intro-body">
            <div class="container">
                <div class="row">
                    <div class="col-md-8 col-md-offset-2">
                        <h1 class="brand-heading">knit</h1>
                        <p class="intro-text">All your best friends in one place.</p>
                        <a href="#about" class="btn btn-circle page-scroll">
                            <i class="fa fa-angle-double-down animated"></i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <section id="about" class="container content-section text-center">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2">
                <h2>About knit</h2>
                <p>knit weaves a close knit social feed with nothing but you favorite people.<br>Create an account and enjoy <a href="#">a different kind of feed.</a> </p><br>
            <% if current_user %>
            <% else %>
            <%= link_to 'Make a new account', new_user_path, {class: "btn btn-default btn-lg"}%>
            <% end %>
                        

            </div>
        </div>
    </section>



        <%#= form_for User.new do |f| %>
        <%#= f.text_field :user_name, placeholder: "pick a username", class:'form-control' %>
        <%#= f.submit :style => "display: none;"%>
        <%# end %>